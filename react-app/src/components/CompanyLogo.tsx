import styled from 'styled-components'

const CompanyLogo = styled.img`
  width: clamp(8.8rem, 50%, 17.6rem);
  height: auto;
  max-width: 100%;
  display: block;
  object-fit: contain;
`

export default CompanyLogo;
