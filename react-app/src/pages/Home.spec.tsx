import "@testing-library/jest-dom/extend-expect";
import {render, screen} from "@testing-library/react";
import Home from "./Home";

describe("Home Page", () => {
  it.todo("It should have a Header");
  it.todo("It should have a Content");
  it.todo("It should have a Pre-footer");
  it.todo("It should have a Footer");
});
