<?php

// NOTE: Add all your custom actions here.

/**
 * @description Action to help debug API routes
 **/

// add_action( 'rest_api_init', function () {
//     $routes = rest_get_server()->get_routes();
//     foreach ( $routes as $route => $data ) {
//         echo rest_url($route) . "\n";
//     }
// } );
